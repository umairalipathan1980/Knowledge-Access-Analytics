# Ultra-minimal requirements - CPU-only for Azure OpenAI setup

# Core web framework  
streamlit>=1.28.0

# LangChain essentials (CPU-only)
langchain>=0.1.0
langchain-core>=0.1.0
langchain-chroma>=0.1.0
langchain-openai>=0.0.8
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1
langgraph>=0.0.40

# Vector database (specify CPU-only version if available)
chromadb>=0.4.18

# Document processing - CPU only
docling>=1.0.0  # Essential for PDF parsing with metadata
PyPDF2>=3.0.1

# Web requests
requests>=2.31.0
beautifulsoup4>=4.12.0

# OpenAI client
openai>=1.0.0

# Essential utilities  
python-dotenv>=1.0.0
pydantic>=2.0.0
typing-extensions>=4.5.0
pysqlite3-binary>=0.5.0
psutil>=5.9.0  # For memory monitoring in containerized environments